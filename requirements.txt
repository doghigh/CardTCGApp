anthropic==0.105.2
PyQt6==6.7.0
opencv-python==4.10.0.84
numpy==1.26.4
Pillow==10.4.0
pypdfium2==4.30.0  # PDF rendering (Apache/BSD license, no external binaries)
requests==2.32.0
beautifulsoup4==4.12.3
pytesseract==0.3.13
reportlab==4.2.0
pytwain==2.3.0; sys_platform == "win32"   # flatbed scanning is Windows/TWAIN only; skipped elsewhere
pyotp==2.9.0
cryptography==43.0.0
qrcode[pil]==8.0
# Note: paddleocr/paddlepaddle removed — never imported (OCR fallback uses
# pytesseract; primary identification is the Claude vision API).